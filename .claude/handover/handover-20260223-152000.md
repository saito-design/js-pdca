# 引継ぎファイル - 2026年2月23日

## 基本ルール参照
- `C:/Users/yasuh/OneDrive/デスクトップ/APP/CLAUDE.md`
- `C:/Users/yasuh/OneDrive/デスクトップ/APP/PDCA/CLAUDE.md`

## 作業中のプロジェクト
**PDCA Dashboard** - 複数企業のPDCAサイクルを管理するWebアプリケーション

## 今回完了した作業

### 1. Python変換スクリプトの整備
- `PDCA/scripts/convert_kagoshima.py` - Excelデータ → JSON変換
- `PDCA/scripts/run_convert.bat` - バッチファイルランチャー（英語版、文字化け対策済み）
- ファイル選択ダイアログ（tkinter）を実装
- 企業ごとに最後に選択したファイルパスを記憶する機能（`convert_config.json`）

### 2. API更新
- `/api/clients/[clientId]/entities/[entityId]/columns/route.ts`
  - master_data.jsonから全カラムを取得（部門・区分フィルタなし）
  - カラム名は「中項目」、区分がある場合は「中項目（区分）」形式

- `/api/clients/[clientId]/entities/[entityId]/chart-data/route.ts`
  - 縦持ち → 横持ち変換（pivotMasterData）
  - フィルタなしで全データを返す

### 3. UIの調整
- **グラフ作成時**: `showOnDashboard: true` をデフォルトに変更
- **ChartRenderer調整**:
  - 凡例フォントサイズ: 10px（2サイズ縮小）
  - グラフ高さ: 220px
  - 凡例との余白を縮小（marginTop: -10）

### 4. 部署ダッシュボード
- chart-data APIからデータ取得するように変更
- 更新ボタンを削除（ユーザー要望）

## データフロー（完成）
```
Excel → Python変換 → Google Drive (master_data.json)
                          ↓
                    columns API → データ項目選択UI
                          ↓
                    chart-data API → グラフ表示
                          ↓
                    ダッシュボードにグラフ表示
```

## 重要な決定事項
- **フィルタなし**: 部門や区分によるフィルタは行わず、全データを表示
- **カラム命名規則**: 実績は「中項目」、それ以外は「中項目（区分）」
- **グラフはデフォルトでダッシュボード表示**: showOnDashboard = true

## 既知の問題・注意点
- 「売上推移」グラフはデータが0のまま（データ項目の設定が必要かも）
- バッチファイルは日本語を使うと文字化けするため英語のみ使用

## 関連ファイル
- `PDCA/src/components/chart-renderer.tsx` - グラフ描画
- `PDCA/src/components/chart-builder.tsx` - グラフ作成UI
- `PDCA/.cache/charts.json` - ローカルグラフ保存
- `PDCA/scripts/` - Python変換スクリプト

## 次にやること（未定）
- 必要に応じてグラフのさらなる調整
- 他企業への展開（convert_xxx.py の追加）
